
开发记录
1总体架构设计
购票请求->多线程异步选座->返回请求 等待处理
              ->轻量级 多线程直接处理       }
              ->消息队列                  }->选座完成后异步创建订单(实现和异步选座一样)
选消息队列  理由1.架构解耦2.高并发需要削峰填谷
保证高性能
难点如何保证高可用 如事务 幂等
后期思路改善 弹性架构
再非高峰期
购票服务直接调用座位和订单服务 直接返回选座结果
高峰期
购票服务->发送消息(包含请求ID)->座位服务->回调消息(包含请求id)->购票服务->发送消息(包含请求id)->订单服务->标记该请求成功 
购票服务->处理检查购票请求状态响应->成功->返回数据
一旦某环节出问题根据请求id回滚
前端
根据收到的响应 
如果包含结果直接渲染 否 加载界面 一定时间再次发送检查请求->成功渲染 否则失败

网关
由于请求流量大 编程目前三个异步非阻塞实现思路
 1.多线程+回调函数
 2.消息队列+消息处理器
 3.事件驱动循环/协程
网络自然是事件驱动循环/协程最好 最终选择webflux的工业选择 基于Netty

时间localtime
1.api方便 能互相运算 能加一减一
2.区分date time
3.未来可能跨时区 后端统一返回时区 前端根据统一的格式化为自己时区的

2.各种方案实现思路
选座具体实现方案
涉及
火车车次 约1w数据
时间   同一车次最多售出30天
城市(实际站台)区间 约3368数据 算一趟火车最多经过3000站台 得区间9000000 10的7次方 实际远远小于
区间状态表示优化：另一种区间维护状态表示 只维护相邻区间 如涉及A-D 那么扣除A-B B-C C-D
车厢  15}
座位  118}->1500个座位
状态表示优化： 单独维护一等座 二等座 商务组等 算最多5种 这个状态表示的前置条件就是 相同车厢必须连接再一起并维护每种车厢的第一个的车厢号 然后用长度为1500的二进制（约1500/8=188 B）表示每个座位的选择情况 并根据车厢大小可以得出车厢号

维护true false有没有被选
车次*区间*时间*总座位=10000*30*10^7*1500 *1B=10^16B=10^13KB=10^10MB=10^7GB=10^4TB 内存溢出风险

优化后=10000*30*3368*188B=2*10^11=200GB 及其悲观情况下 单内存溢出风险 但概率非常小因为是按照最大最大的情况算 redis集群能够存下

但这还不够因为到这成本依然太大
考虑用数据库来维护具体座位占用情况 redis存热点
数据库只存售出的购买情况
维度为 火车车次 车次日期 座位车厢 座位号 座位类型 城市区间(只存一个) 即买a-c只存a-c
后面还可以关联订单详情
redis维护维度为 
1.余票数量 火车车次 车次日期 座位类型 维护余票数量 值区间余票数组 买a-c 扣减 a-b b-c 同理 查询a-c就是a-b b-c的最低值
2.余票详情 火车车次 车次日期 车厢号 值为每个区间的座位占用数组 用比特表示座位具体占用情况 要的到a-c的区间占用情况就把a-b b-c 进行或运算即可 至于string化的比特序列化问题定义一个座位比特占用类 自定义序列化问题
改表的时候利用canvas监听维护redis数据一致性
衍生问题
1.颗粒为火车车次 颗粒是否过大高并发影响？没办法 因为区间购票a-c需要检查其他区间 区间没办法再细分颗粒度 但这个由脚本实现具体的扣减逻辑 redis本身就是多路复用的单线程模型 所以哪怕分的再细哪怕区间不用维护其他区间 redis执行仍然是单线程 使用脚本提交修改已经利用了多路复用
修改
这个衍生最大的问题不是并发度 已经介绍了redis模型
问题在于 每次脚本的计算量从将区间a-b string的比特位计算(这里是将比特序列化成字节流byte[]到redis的) 由于是string 时间复杂度是len(string)*(b-a)
//TODO 但是这个模型key多而分散复杂 打到数据库概率大得多 需要热启动处理 而且不太支持lua选座
3.余票详情 （火车车次 车次日期 座位号）为key 值为每个区间的占用情况
这最主要的问题还是list不支持bit类型  位运算本身可以压到（b-a）的
另一种想法就是拆分值为为list 放到key上面
4.余票详情 （火车车次 车次日期 车厢号 区间） 座位占用
//TODO 最终定夺方案 待实现 目前仍然是快速实现的2模型
5.余票详情 火车车次 车次日期 车厢号 值二维bit图（1110）（1110）代表第1 2个座位的区间占用情况
关于Bit 映射策略
到底是每个座位的区间状态放在一起 还是将每个区间状态的每个座位这样放好
前者再操作设置某个座位状态时极快 但再找出空位时需要遍历每个座位并检查
后者设置某个座位时需要将每个区间的座位状态修改 但是合并？

具体选座架构设计
java：实现各个选座策略的具体实现（包括各种火车绿皮车的硬座 软座 软硬卧 高铁 一二等座商务座）主要根据传入的字符串的座位号 比如A 以及这几种车厢类型的布局选座 额外的还有一个是否连续的策略选择
lua：脚本接受参数座位总数 区间长度 座位索引来查看并选择座位的脚本


那让lua去处理 偏好逻辑？ 是认真的吗？
比方说有卧铺 硬座 一等 商务  偏好不同 结构更加不同
还要考虑是否连续
唯一的方案就是java去处理这个逻辑 用桥接处理各种车厢类型的偏好选座策略和是否连续是否同车厢的偏好策略
然后写各种各样的lua脚本 保证不要过多的if复杂逻辑





搜索线路问题
1.无中转
首先直接搜索火车站点关系表
那么sql语句就是
EXPLAIN  SELECT
             t1.run_date,
             t1.train_id,
             t1.train_number,
             t1.station_name,
             t2.station_name
FROM t_train_station AS t1
JOIN t_train_station as t2 on t1.train_id=t2.train_id
WHERE t1.run_date='2025-01-01'
AND t1.station_name='上海'
AND t2.station_name='北京'
AND t1.sequence<t2.sequence;
无优化情况下假设表一共n个数据
那么连表 时间复杂度 n*n 有train_id索引n*log n 实际因为train_id有多行 复杂度稍高一些
实际优化 无论是否有索引 mysql会建立哈希遍历一个表通过哈希查找 复杂度为 n+n 
考虑where
t1涉及 train_id train_number(select覆盖防止回表) run_date station_name sequence
t2涉及 train_id station_name sequece
分别建立索引 复杂度可为过滤后所有的数据 x+x

如果想快速搜索线路 那我就让一个表专门记录所有线路 让后利用mysqlB+数据结构快速查找
这比只维护车次和站点的表快很多因为这不仅要维护/建模图还要再图进行dfs计算非常慢
如何构建这张表？
依赖车次站点关系表 一次初始化！
加站点和车次怎么办 那就顺便改变此表 耦合？事件编程处理
2.有中转
用户自定义
套用上述中转方案即可
推荐
1.枚举热门中转方案/可达站台 套用上述逻辑

实时余票诞生n+1问题
包装通用safeget批量获取代码
难点 批量redisson锁问题 散点缓存有的 有的无
灵活运用数组索引 匿名指针技巧 完成同批量代码包装
注意点 1.必须对key进行排序 由于连续获取lock非原子性 保证所有线程同一顺序获取 2.一定要去重 否则可能出现同key造成死锁

城市与站台映射关系
一城市含有多站台
n起始站台-m起始站台=m*n个站台组合搜索
搜索线路时
这里思路用原生mysql配合mybatis-plus动态mysql语句foreach 生成(startStation,endStation) in (m*n.....) 避免 and来优化 
TODO配套对应索引提升效率(departureStation,arrivalStation,name) 加name达成覆盖索引

搜索结果字段填充 价格问题


缓存一致性/击穿/穿透/雪崩问题
缓存采用canal监听binlog达到缓存一致性
总结缓存设计原则
两种模式对比总结
维度	底层缓存（Cache-Aside）	结果缓存（View Cache）
缓存内容	DO/Entity（数据库行）	VO/Response（API结果）
Key 设计	主键/唯一ID（如 user:1001）	查询参数哈希（如 search:北京:上海:20251201）
一致性	较高（可控制）	较低（依赖 TTL）
性能	中等（可能多次查）	极高（一次命中）
维护成本	低（结构稳定）	高（缓存失效难）
适用阶段	中台、核心系统	C端高并发、展示型接口
这里选择底层维护 保证简单维护缓存一致性
设计包装方法safeGet通过分布式锁+双重检查锁+回调函数设置避免击穿/雪崩


高峰购票异步化完整技术方案

     基于代码库分析，设计了以下完整的异步化实现方案。

     一、架构设计总览

     高峰流量路径:
     Gateway (TrafficMonitorFilter)
       ↓ 检测到高峰 QPS > 100
       设置 Redis: traffic:peak:status = true (5s TTL)
       ↓
     TicketService.purchase()
       ↓ 读取 PEAK_STATUS_KEY 为 true
       生成 requestId → 存储 t_ticket_async_request (PENDING)
       发送 MQ 消息 → ticket-purchase-topic
       立即返回 {status: "PROCESSING", requestId: "xxx"}
       ↓
     前端轮询 GET /api/ticket/check/{requestId}
       ↓
     TicketPurchaseConsumer 消费消息
       ↓
     AsyncTicketPurchaseService.process()
       ① 选座 (SeatService)
       ② 查列车信息
       ③ 计算票价 (FareCalculationService)
       ④ 创建订单 (OrderService)
       ⑤ 更新 t_ticket_async_request → SUCCESS + orderSn
       失败 → 释放座位 → 更新 t_ticket_async_request → FAILED

     低流量（正常）路径保持现有同步流程不变。

     二、核心设计决策

     1. 查询键设计：requestId 为主，user-train-date 为辅

     - requestId：前端轮询使用，全局唯一，防止越权查询
     - user-train-date：用户端"我的订单"页面对接，已在 t_order 存储

     2. 幂等性分层保障

     ┌──────────┬─────────────────┬──────────────────────────┬──────────┐
     │   层级   │      机制       │           Key            │ 过期时间 │
     ├──────────┼─────────────────┼──────────────────────────┼──────────┤
     │ HTTP请求 │ @Idempotent注解 │ userId-trainNum-date     │ 5分钟    │
     ├──────────┼─────────────────┼──────────────────────────┼──────────┤
     │ MQ消息   │ Redis SET NX    │ ticket:async:{requestId} │ 30分钟   │
     ├──────────┼─────────────────┼──────────────────────────┼──────────┤
     │ 业务逻辑 │ 数据库唯一约束  │ requestId (UNIQUE)       │ 永久     │
     └──────────┴─────────────────┴──────────────────────────┴──────────┘

     3. 座位锁定回滚

     - 选座与订单创建在同一个Consumer事务中
     - 失败时发送 seat-release-topic 释放座位
     - 座位释放失败进入死信队列告警

     4. 消息重试策略

     - 发送端：异步发送 + 失败后落库 t_ticket_async_request 状态为 SEND_FAILED
     - 消费端：RocketMQ默认16次重试，最终进入DLQ

     三、数据库设计

     新建表：t_ticket_async_request

     CREATE TABLE `t_ticket_async_request` (
       `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
       `request_id` VARCHAR(64) NOT NULL COMMENT '请求唯一ID，雪花算法生成',
       `user_id` BIGINT NOT NULL COMMENT '用户ID',
       `train_num` VARCHAR(20) NOT NULL COMMENT '车次号',
       `date` DATE NOT NULL COMMENT '乘车日期',
       `status` TINYINT NOT NULL DEFAULT 0 COMMENT '状态：0-处理中，1-成功，2-失败，3-发送失败',
       `order_sn` VARCHAR(64) DEFAULT NULL COMMENT '成功后的订单号',
       `error_message` TEXT DEFAULT NULL COMMENT '失败原因',
       `request_params` JSON DEFAULT NULL COMMENT '原始请求参数JSON（用于重试）',
       `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
       `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
       PRIMARY KEY (`id`),
       UNIQUE KEY `uk_request_id` (`request_id`),
       KEY `idx_user_train_date` (`user_id`,`train_num`,`date`),
       KEY `idx_status_create_time` (`status`,`create_time`)
     ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='异步购票请求跟踪表';

     四、文件清单

     新建 Java 文件 (7个)

     ┌──────────────────────────────────────────────────────────────────────────────┬──────────────────┐
     │                                   文件路径                                   │       用途       │
     ├──────────────────────────────────────────────────────────────────────────────┼──────────────────┤
     │ Services/ticket-service/.../dto/AsyncTicketPurchaseMessage.java              │ MQ消息体         │
     ├──────────────────────────────────────────────────────────────────────────────┼──────────────────┤
     │ Services/ticket-service/.../dto/response/AsyncTicketCheckVO.java             │ 状态查询响应     │
     ├──────────────────────────────────────────────────────────────────────────────┼──────────────────┤
     │ Services/ticket-service/.../service/AsyncTicketPurchaseService.java          │ 异步购票服务接口 │
     ├──────────────────────────────────────────────────────────────────────────────┼──────────────────┤
     │ Services/ticket-service/.../service/impl/AsyncTicketPurchaseServiceImpl.java │ 异步购票服务实现 │
     ├──────────────────────────────────────────────────────────────────────────────┼──────────────────┤
     │ Services/ticket-service/.../consumer/TicketPurchaseConsumer.java             │ MQ消费者         │
     ├──────────────────────────────────────────────────────────────────────────────┼──────────────────┤
     │ Services/ticket-service/.../entity/TicketAsyncRequestDO.java                 │ 异步请求实体     │
     ├──────────────────────────────────────────────────────────────────────────────┼──────────────────┤
     │ Services/ticket-service/.../mapper/TicketAsyncRequestMapper.java             │ 异步请求Mapper   │
     └──────────────────────────────────────────────────────────────────────────────┴──────────────────┘

     修改 Java 文件 (4个)

     ┌───────────────────────────────┬──────────────────────────────────────────────────────────┐
     │           文件路径            │                         修改内容                         │
     ├───────────────────────────────┼──────────────────────────────────────────────────────────┤
     │ TicketService.java            │ 新增 processCorePurchase() 和 checkStatus() 方法         │
     ├───────────────────────────────┼──────────────────────────────────────────────────────────┤
     │ TicketServiceImpl.java        │ ① 高峰路径调用异步服务 ② 提取核心购票逻辑 ③ 实现 check() │
     ├───────────────────────────────┼──────────────────────────────────────────────────────────┤
     │ PurchaseTicketController.java │ 新增 GET /api/ticket/check/{requestId} 端点              │
     ├───────────────────────────────┼──────────────────────────────────────────────────────────┤
     │ PurchaseTicketVO.java         │ 新增 requestId 字段                                      │
     └───────────────────────────────┴──────────────────────────────────────────────────────────┘

     前端修改

     ┌───────────────────────────────────┬─────────────────────────────────────────────────────┐
     │             文件路径              │                      修改内容                       │
     ├───────────────────────────────────┼─────────────────────────────────────────────────────┤
     │ 12306/types.ts                    │ 新增 requestId 字段和 AsyncTicketCheckResponse 类型 │
     ├───────────────────────────────────┼─────────────────────────────────────────────────────┤
     │ 12306/services/ticketService.ts   │ 新增 checkTicketPurchaseStatus() API                │
     ├───────────────────────────────────┼─────────────────────────────────────────────────────┤
     │ 12306/components/BookingModal.tsx │ 处理 PROCESSING 状态 + 轮询逻辑                     │
     └───────────────────────────────────┴─────────────────────────────────────────────────────┘

     配置文件

     ┌─────────────────┬──────────────────────────────────────┐
     │      文件       │                 修改                 │
     ├─────────────────┼──────────────────────────────────────┤
     │ application.yml │ 添加 ticket-purchase-topic 配置      │
     ├─────────────────┼──────────────────────────────────────┤
     │ createTable.sql │ 导入 t_ticket_async_request 建表语句 │
     └─────────────────┴──────────────────────────────────────┘

     五、关键代码结构

     5.1 MQ消息体

     @Data
     public class AsyncTicketPurchaseMessage implements Serializable {
         private String requestId;
         private Long userId;
         private String account;
         private String trainNum;
         private String startStation;
         private String endStation;
         private String date;
         private List<Long> passengerIds;
         private List<String> seatTypelist;
         private List<String> chooseSeats;
         private Long timestamp;
     }

     5.2 TicketServiceImpl 改造要点

     @Override
     public PurchaseTicketVO purchase(PurchaseTicketRequestDto request, Long userId) {
         String peakStatus = redisTemplate.opsForValue().get(PEAK_STATUS_KEY);
         boolean peakHour = "true".equals(peakStatus);

         if (peakHour) {
             // 高峰模式：异步处理
             String requestId = asyncPurchase(request, userId);
             return PurchaseTicketVO.processing(requestId);
         }

         // 低峰模式：同步处理
         return processCorePurchase(...);
     }

     // 提取的核心购票逻辑（原 purchase 方法主体）
     public PurchaseTicketVO processCorePurchase(
         Long userId, String trainNum, String startStation, String endStation,
         String date, List<Long> passengerIds, List<String> seatTypelist,
         List<String> chooseSeats, String account) {
         // ① 查询乘客
         // ② 调用 seat-service 选座
         // ③ 查列车信息
         // ④ 调用票价计算
         // ⑤ 调用 order-service 创建订单
         // ⑥ 返回结果
     }

     5.3 消费者

     @Component
     @MessageConsumer(topic = "ticket-purchase-topic", tag = "purchase", consumerGroup = "ticket-purchase-consumer")
     @RocketMQMessageListener(topic = "ticket-purchase-topic", consumerGroup = "ticket-purchase-consumer")
     public class TicketPurchaseConsumer extends RocketMQBaseConsumer {
         @Override
         protected void doProcess(Object msg) {
             AsyncTicketPurchaseMessage message = (AsyncTicketPurchaseMessage) msg;
             asyncTicketPurchaseService.processAsyncPurchase(message);
         }
     }

     5.4 Controller 新增接口

     @GetMapping("/check/{requestId}")
     public Result<AsyncTicketCheckVO> checkStatus(
             @PathVariable("requestId") String requestId,
             @RequestHeader("X-User-Id") String userIdHeader) {
         Long userId = Long.parseLong(userIdHeader);
         return Result.success(ticketService.checkStatus(requestId, userId));
     }

     5.5 前端轮询逻辑

     const startPolling = (reqId: string) => {
       const poll = async () => {
         const json = await checkTicketPurchaseStatus(reqId);
         if (json.data?.status === 'SUCCESS') {
           // 跳转支付
         } else if (json.data?.status === 'FAILED') {
           // 显示错误
         } else {
           setTimeout(poll, 2000); // 继续轮询
         }
       };
       poll();
     };

     六、实施步骤

     Phase 1: 数据层

     1. 创建 TicketAsyncRequestDO 实体和 Mapper
     2. 执行建表 SQL

     Phase 2: 服务层

     3. 实现 AsyncTicketPurchaseService
     4. 修改 TicketServiceImpl，分离同步/异步逻辑

     Phase 3: MQ集成

     5. 实现 TicketPurchaseConsumer
     6. 配置 ticket-purchase-topic

     Phase 4: API

     7. 新增 GET /api/ticket/check/{requestId} 接口
     8. 修改 purchase 接口高峰检测逻辑

     Phase 5: 前端

     9. 更新类型定义
     10. 实现轮询逻辑

     七、验证方案

     1. 单元测试：测试 processCorePurchase 核心逻辑
     2. 集成测试：
       - 低峰模式：购票成功返回 SUCCESS
       - 高峰模式：返回 PROCESSING + requestId，轮询后获得结果
     3. 压测：模拟高峰流量，验证 MQ 削峰效果

     八、风险与回滚

     ┌──────────────┬─────────────────────────────┐
     │     风险     │          缓解措施           │
     ├──────────────┼─────────────────────────────┤
     │ MQ消息丢失   │ 异步发送失败落库 + 死信队列 │
     ├──────────────┼─────────────────────────────┤
     │ 座位死锁     │ 订单创建失败立即释放座位    │
     ├──────────────┼─────────────────────────────┤
     │ 前端轮询风暴 │ 指数退避 + 服务端缓存       │
     └──────────────┴─────────────────────────────┘

     回滚方案：
     - 网关关闭高峰检测：traffic.peak-threshold 调高
     - Controller 添加开关：ticket.async.enabled=false 强制走同步




3.架构选型
幂等性解决TODO
消息队列选型 rocketmq
rabbitMq 吞吐量比不过kafaka和rocketmq 而且不支持事务 掉消息的风险大于另外二者
kafka 吞吐量极高 但不支持事务 不能保证强一致性 需自己手写 也异步刷盘 掉消息 也没有重试和死信



4.代码架构实现
包装framework模块
内涵cache database common log idempotent子模块
comman定义全局枚举和常量类型 以及规定包装Result类
cache和database为spring-boot-starter模块 自动导入相关的配置 以及提供相应的封装工具类如cache封装了SafeCacheTemplate来处理上述提到的击穿雪崩并发
log和idemponent用aop实现 加上相应注解即可起到相应作用
5.数据集处理
数据级别 10000列车数据 400w座位 3345站台 列车站台9w 若干票 若干用户 若干订单
数据来源
站台 12306
列车站台
统一用py处理
py随机生成

前端架构选择
2. 为什么 React + Tailwind CSS + Lucide 看起来更“美观”？
你看到的“别人只用这三样就能写出神仙界面”，其实揭示了 React 生态的一个核心优势：“无样式组件（Unstyled Components）” + “实用主义 CSS”。
Vue 的中后台思维（组件库思维）：
Vue 在国内最火的是 Element UI / Ant Design Vue。这些库是“重样式”的。它们给你封装好了一个按钮，自带了阴影、圆角、颜色。
优点：开发极快，风格统一。
缺点：长得都一个样（“后台味”太重），想改得“不一样”非常痛苦，要覆盖很多 CSS 权重。
React 的现代思维（原子化思维）：
你提到的 lucide-react（图标）+ tailwindcss（原子化 CSS），配合像 shadcn/ui 或 Radix UI 这样的无头组件库（Headless UI）。
Headless UI：只提供功能（比如点击弹出、键盘导航），完全不带样式。
Tailwind CSS：让你直接在 HTML 里写样式（比如 bg-black text-white rounded-lg）。
Lucide：提供极简的矢量图标。
为什么这样更美观？
因为这种组合给了开发者100% 的设计自由度。设计师画成什么样，你就能用 Tailwind 拼出什么样，完全不受“默认按钮是蓝色的”这种限制。现在的 Vercel 风格、Linear 风格，大多是这套技术栈堆出来的。
Vue 能做吗？
绝对能。 Vue 3 对 Tailwind CSS 的支持非常好，也有 Headless UI 的 Vue 版本。只是目前国内 Vue 社区还在大量使用 Element Plus 这种“全家桶”组件库，导致大家习惯了“套模板”，而 React 社区更早进入了“手写原子化样式”的阶段。
3. 最终建议：中后台选 Vue，C 端美观选 React？
这个策略非常务实，我为你总结一下这两种路线的“体感差异”：
表格
维度	Vue 3 路线 (Element Plus / Ant Design)	React 路线 (Tailwind + Headless UI)
核心体验	“搭积木”	“捏泥人”
美观上限	中等（受限于组件库主题，容易撞脸）	极高（完全定制化，设计还原度 100%）
开发效率	极高（引入组件就能用，样式不用管）	中等（需要自己写类名，调整间距颜色）
适用场景	管理系统、SaaS 后台、内部工具	官网、C 端 App、SaaS 产品页、跨平台应用
代码画风	<el-button type="primary">提交</el-button>	<button className="bg-blue-500 px-4 py-2 rounded">提交</button>
我的建议：
如果你做的是“功能性”产品（比如 CRM、ERP、数据大屏）：
坚定选 Vue 3 + Element Plus/Ant Design Vue。老板和客户看重的是功能好不好用，数据全不全，界面只要整洁就行。Vue 的开发效率能让你早下班两小时。
如果你做的是“体验性”产品（比如 营销落地页、C 端商城、官网）：
建议选 React + Tailwind CSS。这种项目对动画、细节、交互的定制要求极高，React 的生态（如 Framer Motion 动画库）配合 Tailwind 能让你轻松做出“大厂质感”。
关于 Android 跨平台：
如果你选了 React 做网页，顺便想做个 App，React Native 是无缝衔接的。
如果你选了 Vue，想做 App 通常要用 Uni-app（基于 Vue 语法，编译成小程序或 App），在国内也很流行，但技术原理和 React Native 不同（Uni-app 更多是 WebView 混合或小程序容器）。